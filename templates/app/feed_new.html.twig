{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success float-right" type="submit">Crear noticia</button>
    {{ form_end(form) }}
{% endblock %}

